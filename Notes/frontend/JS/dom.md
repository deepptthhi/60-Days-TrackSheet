

## HTML DOM

DOM represents HTML as objects.

Select elements:

``` js
document.getElementById("demo");
document.querySelector(".box");
```

## Change Content

``` js
element.innerHTML="Hello";
```

## Change CSS

``` js
element.style.color="red";
```

## Events

``` js
button.addEventListener("click",()=>{
 console.log("Clicked");
});
```

## JS Versions

-   ES5
-   ES6 (let, const, arrow functions, classes)
-   Modern ECMAScript updates every year.

## Projects Practice

-   Calculator
-   To-do List
-   Counter
-   Digital Clock
-   Weather App
-   Quiz App

## Quick Revision

-   Use `const` by default.
-   Prefer `===` over `==`.
-   Use functions for reusable code.
-   Manipulate HTML through the DOM.
-   Debug using DevTools.
