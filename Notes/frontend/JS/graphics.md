

## jQuery

``` js
$("#demo").hide();
```

Common methods: - hide() - show() - click() - css() - html()

## Graphics

-   Canvas API
-   SVG

Canvas example:

``` js
const c=document.getElementById("canvas");
const ctx=c.getContext("2d");
ctx.fillRect(20,20,100,100);
```

## Best Practices

-   Prefer const over let.
-   Avoid var.
-   Use === instead of ==.
-   Keep functions small.
-   Handle errors with try...catch.

## Interview Tips

-   Hoisting
-   Closures
-   Event Loop
-   Promises
-   async/await
-   Call, Apply, Bind
-   Prototype
-   this keyword
-   DOM Manipulation
-   Fetch API

## Quick Revision

-   Objects store data.
-   Arrays store ordered values.
-   Classes create objects.
-   Promises handle async work.
-   Fetch gets data from APIs.
-   JSON exchanges data.
